# Copy to a local .env file and provide values through an approved secret store.
# Never commit the populated .env file.

MCP_ENVIRONMENT=development
MCP_AUDIT_LOG_PATH=./var/audit/mcp-events.jsonl

GITHUB_MCP_COMMAND=
GITHUB_MCP_TOKEN=
FIRECRAWL_MCP_ENDPOINT=
FIRECRAWL_API_KEY=
SHOPIFY_MCP_ENDPOINT=
SHOPIFY_MCP_TOKEN=
META_MCP_ENDPOINT=
META_MCP_TOKEN=
HERMES_MCP_COMMAND=
